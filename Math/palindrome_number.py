class Solution:
    def isPalindrome(self, x: int) -> bool:
        """
        Determine if an integer is a palindrome.
        
        This function checks if a given integer reads the same forwards and backwards.
        Negative numbers are not considered palindromes.
        
        Args:
            x (int): The integer to check for palindrome property
            
        Returns:
            bool: True if x is a palindrome, False otherwise
            
        Example:
            >>> solution = Solution()
            >>> solution.isPalindrome(121)
            True
            >>> solution.isPalindrome(-121)
            False
            >>> solution.isPalindrome(10)
            False
            >>> solution.isPalindrome(12321)
            True
            
        Time Complexity: O(log n) where n is the number of digits
        Space Complexity: O(1) - uses only constant extra space
        """
        # TODO: Implement the solution
        pass
